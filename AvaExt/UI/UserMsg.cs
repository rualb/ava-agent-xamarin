using System;
using System.Collections.Generic;
using System.Text;

using AvaExt.Common;

using System.Threading;

namespace AvaExt.UI
{
    //public class UserMsg
    //{
    //    public static bool showConfirm(IEnvironment env, string text)
    //    {
    //        return (MessageBox.Show((env != null) ? env.translate(text) : text,
    //            CurrentVersion.getName(),
    //            MessageBoxButtons.OKCancel,
    //            MessageBoxIcon.Question, MessageBoxDefaultButton.Button2) == DialogResult.OK);

    //    }
    //    public static void showInformation(IEnvironment env, string text)
    //    {
    //          MessageBox.Show((env != null) ? env.translate(text) : text,
    //            CurrentVersion.getName(),
    //            MessageBoxButtons.OK,
    //            MessageBoxIcon.Asterisk, MessageBoxDefaultButton.Button1);

    //    }
    //    public static void showError(IEnvironment env, string text)
    //    {

    //          MessageBox.Show((env != null) ? env.translate(text) : text,
    //            CurrentVersion.getName(),
    //            MessageBoxButtons.OK,
    //            MessageBoxIcon.Hand, MessageBoxDefaultButton.Button1);

    //    }

    //}
}

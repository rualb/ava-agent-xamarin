using System;
using System.Collections.Generic;
using System.Text;
using System.Data;
using MobExt.AndroidEnv.ControlsBase;

namespace MobGE.MobControl.ControlsTools
{
    public class ToolDataGrid  
    {
        public static int getRowIndexBindedTo(DataGrid grid, DataRow row)
        {
            int indx = -1;
            //if (grid != null && row != null)
            //    for (int i = 0; i < grid.RowCount; ++i)
            //        if (object.ReferenceEquals(row, grid.Rows[i].DataBoundItem))
            //            return i;
            return indx;

        }
 

    }
}
